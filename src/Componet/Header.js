import React from "react";
import ThemeTogler from "./ThemeTogler";

const Header = () => {
    return(
        <header>
            <h1>Theme toggler</h1>
            <ThemeTogler/>
        </header>
    )
};

export default Header;
